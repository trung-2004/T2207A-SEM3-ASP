@model List<T2207A_MVC.Entities.Brand>
@{
}
<div class="container">
    <div class="row">
        <div class="col-lg-6">
            <h1>List Brands</h1>
        </div>
        <div class="col-lg-6">
            <a asp-controller="Brand" asp-action="Create" class="btn btn-primary float-end">Add Brand</a>
        </div>

    </div>

    <div class="row">
        <div class="col-lg-12">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Name</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@item.id</th>
                            <td>@item.name</td>
                            <td>
                                <a class="me-2" asp-controller="Brand" asp-action="Edit"><i class="fa-regular fa-pen-to-square"></i></a>
                                <a asp-controller="Brand" asp-action=""><i class="fa-solid fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>